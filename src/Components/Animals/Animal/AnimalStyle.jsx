import {boxShadow} from '../../../utils/constant';

const styles = {
    Animal: {
        width: '60%',
        padding: '25px',
        border: '1px solid #eee',
        borderRadius: '5px',
        boxShadow,
        background: 'white',
        margin: '16px auto',
        fontWeight: 'bolder'
    }
}

export default styles;